import SafePassage from "@/components/SafePassage";

export const dynamic = "force-dynamic";

export default function DallasPassage() {
  return <SafePassage slug="solis-academies" base="/dallas" />;
}
